environments:
  - name: acceptance
  - name: production

service:
  imageRepository: nginx:alpine
  pipeline:
    build:
      disabled: true

extensions:
- url: https://github.com/stelligent/mu-cfn_nag/archive/v0.1.zip
